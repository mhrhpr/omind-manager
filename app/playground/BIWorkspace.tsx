'use client';
import { useMemo, useState } from 'react';
import Link from 'next/link';
import * as XLSX from 'xlsx';
import { profileRows, type Analysis, type Row } from '../../lib/omind-engine';
import { runDataPipeline, type PipelineResult } from '../../lib/data-pipeline';

type Chart={id:string;type:'bar'|'line'|'scatter';title:string;description:string;data:any[]};
function kind(name:string){const s=name.toLowerCase();return s.endsWith('.xlsx')||s.endsWith('.xls')?'excel':s.endsWith('.json')?'json':'csv'}
async function parseFile(file:File):Promise<Row[]>{const b=await file.arrayBuffer();const n=file.name.toLowerCase();if(n.endsWith('.json')){const p=JSON.parse(new TextDecoder().decode(b));const rows=Array.isArray(p)?p:p?.data;if(!Array.isArray(rows))throw new Error('JSON نامعتبر است.');return rows as Row[]}const wb=XLSX.read(b,{type:'array',cellDates:true,raw:true});if(!wb.SheetNames.length)throw new Error('Sheet قابل تحلیل پیدا نشد.');return XLSX.utils.sheet_to_json<Row>(wb.Sheets[wb.SheetNames[0]],{defval:null,raw:true})}
function makeCharts(rows:Row[],a:Analysis|null):Chart[]{if(!rows.length)return[];const cols=a?.columns||[];const measures=cols.filter(c=>c.role==='measure').map(c=>c.name);const dates=cols.filter(c=>c.role==='date').map(c=>c.name);const dims=cols.filter(c=>c.role==='dimension'||c.role==='category').map(c=>c.name);const out:Chart[]=[];const m=measures[0];if(dates[0]&&m){const g:Record<string,number[]>={};rows.forEach(r=>{const d=String(r[dates[0]]||'').slice(0,10);const v=Number(r[m]);if(d&&Number.isFinite(v)) (g[d]??=[]).push(v)});const p=Object.entries(g).sort((x,y)=>x[0].localeCompare(y[0]));if(p.length>2)out.push({id:'trend',type:'line',title:`روند ${m}`,description:'روند شاخص در طول زمان',data:p.slice(-30).map(([label,v])=>({label,value:Number((v.reduce((x,y)=>x+y,0)/v.length).toFixed(2))}))})}if(dims[0]&&m){const g:Record<string,number[]>={};rows.forEach(r=>{const k=String(r[dims[0]]??'').trim(),v=Number(r[m]);if(k&&Number.isFinite(v))(g[k]??=[]).push(v)});const p=Object.entries(g).map(([label,v])=>({label,value:Number((v.reduce((x,y)=>x+y,0)/v.length).toFixed(2))})).sort((a,b)=>b.value-a.value).slice(0,8);if(p.length>1)out.push({id:'comparison',type:'bar',title:`${m} بر اساس ${dims[0]}`,description:'مقایسه عملکرد گروه‌ها',data:p})}if(measures.length>1){const a1=measures[0],a2=measures[1];const p=rows.map(r=>({x:Number(r[a1]),y:Number(r[a2])})).filter(p=>Number.isFinite(p.x)&&Number.isFinite(p.y)).slice(0,180);if(p.length>7)out.push({id:'relationship',type:'scatter',title:`${a1} × ${a2}`,description:'رابطه مشاهده‌شده بین دو شاخص',data:p})}return out}
function ChartView({chart}:{chart:Chart}){const w=620,h=250,p=34;const max=Math.max(...chart.data.map(d=>chart.type==='scatter'?d.y:d.value),1),min=chart.type==='scatter'?Math.min(...chart.data.map(d=>d.y)):0;if(chart.type==='bar')return <svg viewBox={`0 0 ${w} ${h}`} className="bi-chart"><line x1={p} y1={h-p} x2={w-p} y2={h-p} className="axis"/>{chart.data.map((d,i)=><g key={i}><rect x={p+i*((w-2*p)/chart.data.length)+8} y={h-p-(d.value/max)*(h-2*p)} width={Math.max(9,(w-2*p)/chart.data.length-16)} height={(d.value/max)*(h-2*p)} rx="7" className="bar"/><text x={p+i*((w-2*p)/chart.data.length)+((w-2*p)/chart.data.length)/2} y={h-12} textAnchor="middle">{String(d.label).slice(0,8)}</text></g>)}</svg>;if(chart.type==='line'){const maxV=Math.max(...chart.data.map(d=>d.value),1),pts=chart.data.map((d,i)=>{const x=p+i*((w-2*p)/Math.max(1,chart.data.length-1));const y=h-p-(d.value/maxV)*(h-2*p);return [x,y] as [number,number]});return <svg viewBox={`0 0 ${w} ${h}`} className="bi-chart"><polyline points={pts.map(x=>x.join(',')).join(' ')} fill="none" className="line"/>{pts.map((q,i)=><circle key={i} cx={q[0]} cy={q[1]} r="4" className="dot"/> )}</svg>}const sx=(x:number)=>p+((x-Math.min(...chart.data.map(d=>d.x),0))/(Math.max(...chart.data.map(d=>d.x))-Math.min(...chart.data.map(d=>d.x))||1))*(w-2*p);const sy=(y:number)=>h-p-((y-min)/(max-min||1))*(h-2*p);return <svg viewBox={`0 0 ${w} ${h}`} className="bi-chart">{chart.data.map((d,i)=><circle key={i} cx={sx(d.x)} cy={sy(d.y)} r="4" className="dot"/> )}</svg>}
export default function BIWorkspace(){const[file,setFile]=useState<File|null>(null),[q,setQ]=useState(''),[role,setRole]=useState('manager'),[pipeline,setPipeline]=useState<PipelineResult|null>(null),[analysis,setAnalysis]=useState<Analysis|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState(''),[tab,setTab]=useState('overview');const theme=file?kind(file.name):'default';const charts=useMemo(()=>makeCharts(pipeline?.cleanedRows||[],analysis),[pipeline,analysis]);async function run(){if(!file){setError('فایل داده را انتخاب کن.');return}setBusy(true);setError('');try{const raw=await parseFile(file);const p=runDataPipeline(raw);if(!p.cleanedRows.length)throw new Error('داده قابل تحلیل نیست.');const a=profileRows(p.cleanedRows,q,role);setPipeline(p);setAnalysis(a)}catch(e){setError(e instanceof Error?e.message:'خطای تحلیل')}finally{setBusy(false)}}return <main className={`bi-app bi-${theme}`}><aside className="bi-sidebar"><Link href="/" className="bi-logo"><span>O</span><strong>OMIND</strong><small>Decision Intelligence</small></Link><div className="bi-workspace"><small>WORKSPACE</small><b>Data Intelligence</b><span>REAL DATA</span></div><nav>{[['overview','⌂','نمای کلی'],['analysis','◫','تحلیل داده'],['signals','⌁','سیگنال‌ها'],['model','◇','مدل داده'],['decisions','✓','تصمیم‌ها']].map(x=><button key={x[0]} onClick={()=>setTab(x[0])} className={tab===x[0]?'active':''}><i>{x[1]}</i>{x[2]}</button>)}</nav><div className="bi-side-foot"><span>OMIND ENGINE</span><b>DATA → SIGNAL → DECISION</b></div></aside><section className="bi-content"><header className="bi-topbar"><div><span>DATASET</span><b>{file?.name||'هنوز دیتایی وارد نشده'}</b></div><div className="bi-top-actions"><span className="bi-theme-chip">THEME · {theme.toUpperCase()}</span><Link href="/" className="bi-home">خانه ↗</Link></div></header><div className="bi-header"><div><span className="bi-kicker">POWER ANALYTICS · OMIND</span><h1>{analysis?.priorities?.[0]||'داشبورد تصمیم‌محور داده'}</h1><p>{analysis?'داده تحلیل شده؛ از KPI به signal و از signal به اقدام حرکت کن.':'یک فایل وارد کن و OMIND برایت یک dashboard تحلیلی خودکار می‌سازد.'}</p></div><div className="bi-health"><small>DATA HEALTH</small><b>{analysis?.health??'—'}</b><span>CONFIDENCE {analysis?.confidence??'—'}%</span></div></div><section className="bi-import"><label><input type="file" accept=".csv,.xlsx,.xls,.json" onChange={e=>{setFile(e.target.files?.[0]||null);setPipeline(null);setAnalysis(null);setError('')}}/><span>↑</span><b>{file?'فایل انتخاب شد: '+file.name:'Excel / CSV / JSON'}</b><small>نسخه اصلی حفظ می‌شود · نسخه پاک‌سازی‌شده برای تحلیل ساخته می‌شود</small></label><div><select value={role} onChange={e=>setRole(e.target.value)}><option value="manager">مدیریت</option><option value="sales">فروش</option><option value="finance">مالی</option><option value="hr">منابع انسانی</option><option value="ops">عملیات</option></select><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="سؤال: چرا فروش افت کرده؟"/><button onClick={run} disabled={!file||busy}>{busy?'در حال تحلیل…':'ساخت داشبورد →'}</button></div></section>{error&&<div className="bi-error">{error}</div>}{analysis&&pipeline&&<><div className="bi-kpis"><div><small>RECORDS</small><b>{pipeline.cleaning.outputRows}</b><span>clean rows</span></div><div><small>SIGNALS</small><b>{analysis.signals.length}</b><span>detected</span></div><div><small>VALIDATION</small><b>{pipeline.validation.score}</b><span>data quality</span></div><div><small>DECISION READY</small><b>{analysis.priorities.length? 'YES':'CHECK'}</b><span>next move</span></div></div><section className="bi-dashboard-grid"><div className="bi-card bi-wide"><div className="bi-card-head"><div><span>ANALYTICS</span><h2>تحلیل خودکار</h2></div><small>{charts.length} visual generated</small></div>{charts.length?<div className="bi-chart-grid">{charts.map(c=><article key={c.id}><h3>{c.title}</h3><p>{c.description}</p><ChartView chart={c}/></article>)}</div>:<div className="bi-empty">با این ساختار داده، نمودار کافی برای تولید خودکار وجود ندارد.</div>}</div><div className="bi-card"><span>SIGNALS</span><h2>چه چیزی مهم است؟</h2>{analysis.signals.slice(0,5).map(s=><div className="bi-signal" key={s.title}><i className={s.priority}/><div><b>{s.title}</b><small>{s.detail}</small></div><strong>{s.score}</strong></div>)}</div><div className="bi-card bi-dark"><span>NEXT MOVE</span><h2>{analysis.actions[0]}</h2><p>{analysis.hypotheses[0]||'قبل از مداخله گسترده، evidence را روی segment مشخص بررسی کن.'}</p><button>ثبت تصمیم ↗</button></div></section><details className="bi-tech"><summary>Technical layer · cleaning · validation · semantic model</summary><div className="bi-tech-grid"><div><b>{pipeline.cleaning.removedDuplicateRows}</b><span>duplicates removed</span></div><div><b>{pipeline.cleaning.normalizedCells}</b><span>cells normalized</span></div><div><b>{modelText(pipeline)}</b><span>grain</span></div><div><b>{pipeline.model.measures.length}</b><span>measures</span></div></div></details></>}</section></main>}
function modelText(p:PipelineResult){return p.model.grain==='each record is one row'?'1 row':'derived'}
